import 'package:fluent_ui/fluent_ui.dart';

class WelcomeToDashboardTabView extends StatelessWidget {
  const WelcomeToDashboardTabView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Welcome to TCMS Dashboard"),
    );
  }
}